{
  "name": "@youzi9601/mcp-testkit-http",
  "version": "0.1.4",
  "description": "HTTP transport for MCP test servers",
  "keywords": [
    "mcp",
    "http",
    "transport",
    "http-transport",
    "model-context-protocol"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "type": "module",
  "main": "./dist/index.js",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No linter configured'"
  },
  "peerDependencies": {
    "vitest": ">=2.0.0 <4.0.0",
    "@youzi9601/mcp-testkit": "workspace:*"
  },
  "dependencies": {
    "@vitest/runner": "^4.1.8",
    "@vitest/utils": "^4.1.8"
  },
  "devDependencies": {
    "vitest": "3.2.6",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Youzi9601/mcp-testkit"
  }
}
